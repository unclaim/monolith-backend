# Модульный Монолит Go-приложения "Chegonado"
Это репозиторий для высокомодульного Go-приложения, разработанного по принципам Domain-Driven Design (DDD) и Архитектуры Портов и Адаптеров (Hexagonal Architecture). Приложение спроектировано как монолит, готовый к будущему выделению в микросервисы, с поддержкой полиглотного постоянства (использования различных баз данных для разных доменов).
Приложение имитирует функциональность платформы "Chegonado", связывая заказчиков с исполнителями для выполнения различных задач, а теперь также включает геймификацию для повышения вовлеченности пользователей.
Важно: Это чисто бэкенд-приложение, предназначенное для работы в связке с клиентским React-приложением. Взаимодействие между фронтендом и бэкендом происходит исключительно через HTTP API.
# 🚀 Основные Технологии и Особенности
- Go (Golang): Основной язык разработки.
- Модульный Монолит: Архитектура, позволяющая сохранять преимущества монолита и при этом подготавливать его к легкому - разделению на микросервисы.
- Полиглотное Постоянство: Различные домены могут использовать оптимальные для них базы данных (например, PostgreSQL, MongoDB, Redis).
- Событийно-ориентированное Взаимодействие: Домены обмениваются данными через внутреннюю событийную шину (in-memory Event Bus) для снижения связанности.
- Логирование: Используется только стандартная библиотека log/slog для структурированного логирования. Все комментарии и логи на русском языке.
- Роутер: Приложение использует только стандартный HTTP-роутер Go.
- Сессии: Управление состоянием пользователя осуществляется через сессии, хранимые в базе данных (например, Redis) для масштабируемости.
- CSRF-токены: Для защиты от атак типа Cross-Site Request Forgery используются токены CSRF.
- Защита от брутфорса: Реализована защита от подбора паролей аналогично принципу работы iPhone. После нескольких неудачных попыток ввода учетных данных для конкретного пользователя или IP-адреса, время блокировки доступа прогрессивно увеличивается.
vБаза данных: Для работы с PostgreSQL используется драйвер pgxpool для эффективного управления пулом соединений.
- Оркестрация: Для локального развертывания баз данных используется Docker Compose.
- Миграции БД: Управление схемами баз данных осуществляется с помощью инструмента migrate.
- Геймификация: Включены домены для управления уровнями пользователей (XP) и игровыми элементами/достижениями.
- API Gateway (внутренний): Реализован логический API Gateway с разделением маршрутов и применением middleware для администраторов и обычных пользователей.
- Безопасность: Применены обширные меры безопасности, включая строгую валидацию данных, авторизацию на основе ролей, шифрование чувствительных данных в БД, HTTPS/TLS, защиту от инъекций и контроль доступа к ссылкам.
- Пользовательские роли: В системе реализована гибкая система ролей, позволяющая назначать пользователям различные уровни доступа и привилегий (например, Заказчик, Исполнитель, Администратор).
- Процесс регистрации: Регистрация пользователя происходит в два этапа. Сначала пользователь вводит свой адрес электронной почты. Система генерирует шестизначный код подтверждения с использованием криптографически стойкого генератора случайных чисел и отправляет его на указанный email. После успешного ввода кода пользователь может задать свое имя, фамилию и уникальный username.
- Процесс входа: Для входа в систему пользователь может использовать либо свой username, либо адрес электронной почты в комбинации с паролем.
- Восстановление пароля: Процесс восстановления пароля также двухэтапный. Пользователь указывает свой адрес электронной почты, на который отправляется шестизначный код подтверждения. После успешного ввода этого кода пользователю предоставляется возможность установить новый пароль, включая его подтверждение.
- Документация API: Используется Swaggo для автоматической генерации Swagger/OpenAPI 2.0 документации на основе комментариев в коде.
# 🌳 Структура Проекта
Проект организован в соответствии со стандартным Go Project Layout, с акцентом на доменное разделение и принципы Гексагональной Архитектуры (Порты и Адаптеры).
```tree
your-ultra-scalable-monolith/
├── cmd/
│   └── server/
│       └── main.go                  # Главная точка входа в приложение. Инициализирует общие зависимости и запускает App Bootstrap.
├── internal/
│   ├── auth/                        # Домен: Аутентификация и Авторизация пользователей.
│   │   ├── api/                     # HTTP-обработчики для внешних запросов (например, регистрация, вход).
│   │   │   └── handlers.go
│   │   ├── domain/                  # Чистая бизнес-логика и сущности домена.
│   │   │   ├── auth.go              # Определение структур для учетных данных, сессий.
│   │   │   ├── ports.go             # Интерфейсы (порты) домена: AuthService (что домен предоставляет), AuthRepository (что домен использует).
│   │   │   └── service.go           # Реализация AuthService, использующая AuthRepository.
│   │   ├── infra/                   # Адаптеры к внешним системам (реализация портов).
│   │   │   └── repository.go        # Реализация AuthRepository (например, PostgreSQL).
│   │   └── events.go                # Определение событий, публикуемых этим доменом (напр., UserRegisteredEvent).
│   ├── users/                       # Домен: Управление Профилями Пользователей (заказчики, исполнители).
│   │   ├── api/
│   │   │   └── handlers.go          # HTTP-обработчики для управления профилями.
│   │   ├── domain/
│   │   │   ├── user.go              # Структуры User, PerformerProfile, CustomerProfile.
│   │   │   ├── ports.go             # Интерфейсы (порты) домена: UserService, UserRepository, UserEventPublisher.
│   │   │   └── service.go           # Реализация UserService, использующая UserRepository.
│   │   ├── infra/
│   │   │   └── repository.go        # Реализация UserRepository (PostgreSQL).
│   │   └── events.go                # События домена (напр., UserProfileUpdatedEvent).
│   ├── tasks/                       # Домен: Управление Задачами.
│   │   ├── api/
│   │   │   └── handlers.go          # HTTP-обработчики для задач.
│   │   ├── domain/
│   │   │   ├── task.go              # Структуры Task, TaskCategory.
│   │   │   ├── ports.go
│   │   │   └── service.go           # Бизнес-логика (создание, обновление, поиск, закрытие задач).
│   │   ├── infra/
│   │   │   └── repository.go        # Реализация репозитория (PostgreSQL или другой).
│   │   └── events.go                # События домена (напр., TaskCreatedEvent, TaskAssignedEvent, TaskApprovedEvent).
│   ├── bids/                        # Домен: Управление Ставками Исполнителей.
│   │   ├── api/
│   │   │   └── handlers.go          # HTTP-обработчики для ставок.
│   │   ├── domain/
│   │   │   ├── bid.go               # Структура Bid.
│   │   │   ├── ports.go
│   │   │   └── service.go           # Бизнес-логика (размещение ставки, принятие/отклонение).
│   │   ├── infra/
│   │   │   └── repository.go        # Реализация репозитория (PostgreSQL).
│   │   └── events.go                # События домена (напр., BidPlacedEvent, BidAcceptedEvent).
│   ├── payments/                    # Домен: Платежи.
│   │   ├── api/
│   │   │   └── handlers.го          # HTTP-обработчики для платежных операций.
│   │   ├── domain/
│   │   │   ├── payment.go           # Структуры Transaction, PaymentDetails.
│   │   │   ├── ports.go
│   │   │   └── service.go           # Бизнес-логика платежей (депонирование, выплаты, комиссии).
│   │   ├── infra/
│   │   │   ├── repository.go        # Реализация репозитория (PostgreSQL для транзакций).
│   │   │   └── payment_gateway_adapter.go # Адаптер для взаимодействия с внешними платежными системами.
│   │   └── events.go                # События домена (напр., PaymentSuccessfulEvent).
│   ├── notifications/               # Домен: Уведомления.
│   │   ├── api/
│   │   │   └── handlers.go          # Если есть API для получения уведомлений.
│   │   ├── domain/
│   │   │   ├── notification.go      # Структура Notification.
│   │   │   ├── ports.go
│   │   │   └── service.go           # Бизнес-логика (создание, агрегация, отправка уведомлений).
│   │   ├── infra/
│   │   │   ├── repository.go        # Реализация репозитория (например, для MongoDB или Redis).
│   │   │   └── email_adapter.go     # Адаптер для отправки электронной почты.
│   │   └── events.go                # События домена (напр., NotificationSentEvent).
│   ├── chat/                        # Домен: Обмен сообщениями в реальном времени.
│   │   ├── api/handlers.go          # WebSocket-обработчики для чата.
│   │   ├── domain/chat.go
│   │   ├── domain/ports.go
│   │   └── domain/service.go
│   │   └── infra/repository.go
│   ├── filestorage/                 # Домен: Управление файловым хранилищем.
│   │   ├── api/handlers.go          # Обработчики для загрузки/скачивания файлов.
│   │   ├── domain/file.go
│   │   ├── domain/ports.go
│   │   └── domain/service.go
│   │   └── infra/s3_adapter.go      # Адаптер для S3-совместимого хранилища.
│   ├── search/                      # Домен: Полнотекстовый поиск.
│   │   ├── api/handlers.go          # HTTP-обработчики для поисковых запросов.
│   │   ├── domain/search.go
│   │   ├── domain/ports.go
│   │   └── domain/service.go
│   │   └── infra/elasticsearch_adapter.go # Адаптер для Elasticsearch.
│   ├── analytics/                   # Домен: Сбор и обработка аналитических данных.
│   │   ├── domain/analytics.go
│   │   ├── domain/ports.go
│   │   └── domain/service.go
│   │   └── infra/repository.go
│   ├── shortener/                   # Домен: Сервис сокращения и защиты ссылок.
│   │   ├── api/handlers.go
│   │   ├── domain/short_link.go
│   │   ├── domain/ports.go
│   │   └── domain/service.go
│   │   └── infra/repository.go
│   ├── reviews/                     # Домен: Отзывы о задачах и исполнителях.
│   │   ├── api/
│   │   │   └── handlers.go          # HTTP-обработчики для создания/получения отзывов.
│   │   ├── domain/
│   │   │   ├── review.go            # Структура Review.
│   │   │   ├── ports.go
│   │   │   └── service.go           # Бизнес-логика (создание, модерация, получение отзывов).
│   │   ├── infra/
│   │   │   └── repository.go        # Реализация репозитория (PostgreSQL).
│   │   └── events.go                # События домена (напр., ReviewCreatedEvent).
│   ├── ratings/                     # Домен: Рейтинги пользователей/исполнителей.
│   │   ├── api/
│   │   │   └── handlers.go          # HTTP-обработчики для получения рейтингов.
│   │   ├── domain/
│   │   │   ├── rating.go            # Структура Rating.
│   │   │   ├── ports.go
│   │   │   └── service.go           # Бизнес-логика (расчет и обновление рейтингов).
│   │   ├── infra/
│   │   │   └── repository.go        # Реализация репозитория (PostgreSQL).
│   │   └── events.go                # События домена (напр., RatingUpdatedEvent).
│   ├── levels/                      # НОВЫЙ ДОМЕН: Уровни пользователя и опыт (XP).
│   │   ├── api/
│   │   │   └── handlers.go          # Эндпоинты для получения уровня/опыта пользователя.
│   │   ├── domain/
│   │   │   ├── level.go             # Сущности для опыта (XP), уровней, конфигурации уровней.
│   │   │   ├── ports.go
│   │   │   └── service.go           # Бизнес-логика начисления опыта, повышения уровня.
│   │   ├── infra/
│   │   │   └── repository.go        # Реализация репозитория (PostgreSQL).
│   │   └── events.go                # События домена (напр., ExperienceGainedEvent, LevelUpEvent).
│   ├── gamification/                # НОВЫЙ ДОМЕН: Игровые элементы, достижения и награды.
│   │   ├── api/
│   │   │   └── handlers.go          # Эндпоинты для получения достижений, информации о квестах.
│   │   ├── domain/
│   │   │   ├── gamification.go      # Сущности для достижений, значков, квестов, наград.
│   │   │   ├── ports.go
│   │   │   └── service.go           # Бизнес-логика выдачи достижений, управления наградами.
│   │   ├── infra/
│   │   │   └── repository.go        # Реализация репозитория (PostgreSQL или MongoDB).
│   │   └── events.go                # События домена (напр., AchievementUnlockedEvent, RewardGrantedEvent).
│   ├── middleware/                  # Общие HTTP-middleware, применяемые к запросам.
│   │   ├── auth_middleware.go       # Проверка аутентификации и авторизации.
│   │   ├── csrf_middleware.go       # Проверка CSRF-токенов.
│   │   └── rate_limit_middleware.go # Защита от брутфорса и лимитирование запросов.
│   ├── shared/                      # Общие элементы, специфичные для этого приложения, не привязанные к конкретному домену.
│   │   ├── config/                  # Загрузка и управление общей конфигурацией приложения.
│   │   │   └── config.go
│   │   ├── common_errors/           # Общие пользовательские типы ошибок приложения.
│   │   │   └── errors.go            # Содержит общие ошибки приложения и/или базовые структуры для доменных ошибок.
│   │   ├── validator/               # Общая утилита для валидации входных данных.
│   │   │   └── validator.go
│   │   └── util/                    # Вспомогательные функции, специфичные для "Chegonado".
│   │       └── helpers.go
│   ├── app_bootstrap.go             # Файл для инициализации всех доменов, их зависимостей (связывание портов и адаптеров) и регистраций событий.
│   └── routes.go                    # Централизованное определение всех HTTP-маршрутов приложения.
├── docs/                            # Сгенерированные файлы Swagger/OpenAPI документации (Swaggo)
│   ├── swagger.json                 # JSON-файл с документацией
│   ├── swagger.yaml                 # YAML-файл с документацией
│   └── docs.go                      # Автоматически сгенерированный Swaggo-файл инициализации
├── pkg/                             # Внешние, переиспользуемые библиотеки, которые могут быть выделены в отдельные Go-модули.
│   ├── infrastructure/              # Общие инфраструктурные компоненты.
│   │   ├── database/                # Код для установления соединений с различными базами данных.
│   │   │   ├── postgres.go          # Инициализация pgxpool для PostgreSQL.
│   │   │   ├── mongodb.go           # Инициализация MongoDB-клиента.
│   │   │   └── redis.go             # Инициализация Redis-клиента (для кэша, сессий, rate limiting).
│   │   ├── logger/
│   │   │   └── logger.go            # Общая утилита для структурированного логирования на базе log/slog.
│   │   ├── eventbus/                # Интерфейс и in-memory реализация Event Bus для обмена событиями.
│   │   │   └── eventbus.go
│   │   ├── metrics/                 # Модуль для сбора метрик (например, Prometheus).
│   │   │   └── prometheus.go
│   │   └── healthcheck/             # Модуль для эндпоинтов проверок здоровья.
│   │       └── healthcheck.go
│   ├── security/                    # Общие утилиты безопасности (хеширование, сессии, CSRF).
│   │   ├── jwt.go                   # Функции для работы с JWT (если используются).
│   │   ├── password.go              # Хэширование и проверка паролей.
│   │   ├── session.go               # Управление HTTP-сессиями.
│   │   ├── csrf.go                  # Генерация и валидация CSRF-токенов.
│   │   └── data_encryption.go       # Утилиты для шифрования/дешифрования данных.
│   ├── paymentprovider/             # Интерфейсы для взаимодействия с платежными провайдерами.
│   │   └── provider.go
│   ├── email/
│   │   └── mailgun.go               # Утилита для отправки электронной почты.
│   ├── websocket/                   # Вспомогательные функции для работы с WebSocket-соединениями.
│   │   └── websocket.go
│   └── pubsub/                      # Дополнительные утилиты для Pub/Sub (если не Event Bus, а внешний брокер).
│       └── redis.go
├── web/                             # Каталог для статических файлов, если бэкенд также обслуживает фронтенд.
│   ├── static/                      # Статические файлы: CSS, JavaScript, изображения.
│   │   ├── css/
│   │   ├── js/
│   │   └── img/
│   └── templates/                   # HTML-шаблоны для серверного рендеринга (если есть).
│       └── ...
├── migrations/                      # SQL-скрипты миграций для баз данных.
│   ├── postgres/                    # Миграции для PostgreSQL.
│   │   ├── 000001_create_users_table.up.sql
│   │   └── 000001_create_users_table.down.sql
│   ├── mongodb/                     # Миграции (скрипты инициализации) для MongoDB.
│   │   └── 000001_init_collections.js
│   └── elasticsearch/               # Скрипты инициализации индексов для Elasticsearch.
│       └── 000001_init_tasks_index.json
├── deploy/                          # Файлы для продакшн-развертывания
│   ├── kubernetes/                  # Манифесты Kubernetes
│   │   ├── deployment.yaml          # Определение Deployment для вашего Go-приложения
│   │   ├── service.yaml             # Определение Service для доступа к приложению
│   │   ├── ingress.yaml             # Определение Ingress для внешнего HTTP/S доступа
│   │   ├── secret.yaml.example      # ПРИМЕР: Kubernetes Secret для чувствительных данных (НЕ КОММИТЬ В GIT!)
│   │   └── configmap.yaml           # Kubernetes ConfigMap для нечувствительных настроек
│   ├── prometheus/                  # Конфигурация для Prometheus/Grafana
│   │   ├── rules.yaml               # Правила для Alertmanager (алерты о проблемах)
│   │   └── grafana-dashboard.json   # Пример JSON-определения дашборда Grafana
│   └── scripts/                     # Вспомогательные скрипты для деплоя
│       ├── run-migrations.sh        # Скрипт для запуска миграций (может быть частью CI/CD)
│       └── healthcheck.sh           # Простой скрипт для проверки доступности сервиса
├── .env.example                     # Пример файла переменных окружения (НЕ КОММИТИТЬ .env!)
├── docker-compose.yml               # Описание сервисов Docker Compose для локальной разработки.
├── Dockerfile                       # Dockerfile для сборки образа приложения Go.
├── go.mod                           # Модуль Go, управляющий зависимостями проекта.
├── go.sum                           # Контрольные суммы зависимостей.
└── README.md                        # Этот файл.
```

# 🔒 Безопасность и Аутентификация
В приложении реализован комплекс мер безопасности для защиты пользовательских данных и предотвращения несанкционированного доступа:
- Защита от брутфорса: Система активно противодействует атакам методом перебора паролей. Принцип работы схож с блокировкой iPhone: после нескольких последовательных неудачных попыток входа для одной учетной записи или с одного IP-адреса, доступ временно блокируется. Длительность блокировки прогрессивно увеличивается с каждой новой попыткой. Это помогает снизить риск компрометации учетных записей.
- Ролевая модель: Приложение поддерживает несколько ролей пользователей (например, Заказчик, Исполнитель, Администратор), каждая из которых обладает определенными правами доступа к функционалу. Это обеспечивает гранулированный контроль над операциями и данными.
Двухэтапная регистрация:
- Ввод Email: Пользователь предоставляет свой адрес электронной почты. Система проверяет, что указанный адрес не является временным (одноразовым) почтовым ящиком, используя внутренний список или внешний сервис.
- Подтверждение Email: На указанный email отправляется уникальный шестизначный код, сгенерированный с использованием криптографически надежного метода. Пользователь должен ввести этот код для подтверждения владения почтой.
- Создание профиля: После успешной проверки кода, пользователю предлагается ввести имя, фамилию и выбрать уникальный username для своей учетной записи.
- Гибкий вход в систему: Пользователи могут войти в систему, используя либо свой зарегистрированный username, либо адрес электронной почты, в сочетании с паролем.
- Четырехзначный PIN-код для входа: После успешной регистрации и входа в систему, пользователь может установить опциональный четырехзначный PIN-код в настройках своего аккаунта. Этот PIN-код может быть использован для быстрого входа на доверенных устройствах, предоставляя дополнительный уровень удобства и безопасности.
Восстановление пароля:
- Запрос на восстановление: Пользователь указывает email, связанный с его учетной записью.
Подтверждение по коду: На этот email отправляется шестизначный код подтверждения.
- Смена пароля: После успешного ввода кода, пользователю предоставляется защищенная форма для ввода нового пароля и его подтверждения.
- CSRF-токены: Для защиты от атак типа Cross-Site Request Forgery используются токены CSRF, которые генерируются и проверяются на стороне сервера.
Шифрование данных: Чувствительные данные (например, пароли) хранятся в зашифрованном виде в базе данных.
HTTPS/TLS: Все взаимодействия с API должны происходить по защищенному протоколу HTTPS, обеспечивая шифрование трафика.
# 📦 Описание Модулей
Эта секция подробно описывает назначение и содержимое основных модулей проекта.
cmd/
- Назначение: Содержит исполняемые файлы (main package) для различных версий или сервисов приложения.
cmd/server/main.go: Главная точка входа в монолитное приложение. Отвечает за общую инициализацию приложения, такую как загрузка конфигурации, настройка логгера, а затем делегирует оркестрацию зависимостей и запуск HTTP-сервера файлу internal/app_bootstrap.go.
internal/
- Назначение: Код, который является приватным для этого приложения и не предназначен для использования другими проектами Go. Здесь реализуются домены приложения, их бизнес-логика и адаптеры, а также общие компоненты, специфичные для этого монолита.
internal/ДОМЕН/ (e.g., auth, users, tasks, bids, etc.)
- Назначение: Каждый каталог представляет собой независимый домен в рамках архитектуры DDD. Домены инкапсулируют свою бизнес-логику, сущности и правила, взаимодействуя с внешним миром через интерфейсы (порты).
api/handlers.go: Содержит HTTP-обработчики (контроллеры), которые принимают входящие запросы, валидируют их, преобразуют в команды домена и вызывают соответствующий доменный сервис. Также отвечает за форматирование ответов.
domain/ДОМЕН.go: Определяет основные сущности (агрегаты, Value Objects) этого домена. Эти структуры представляют собой чистые данные и бизнес-правила, не зависящие от инфраструктуры.
domain/ports.go: Ключевой файл в гексагональной архитектуре. Определяет интерфейсы (порты), через которые доменный слой взаимодействует с внешним миром:
Входящие порты (Driving Ports): Интерфейсы, которые доменный сервис предоставляет для использования внешними адаптерами (например, API-обработчиками). Пример: UserService с методами GetUserProfile, CreateUser.
Исходящие порты (Driven Ports): Интерфейсы, которые доменный сервис использует для взаимодействия с внешними системами (например, базами данных, внешними сервисами). Пример: UserRepository, EventPublisher.
domain/service.go: Содержит основную чистую бизнес-логику домена. Этот файл реализует входящие порты (например, UserService) и использует исходящие порты (например, UserRepository), чтобы выполнить операции. Этот слой не зависит от деталей инфраструктуры.
infra/repository.go (и другие адаптеры): Содержит реализации исходящих портов. Это адаптеры, которые связывают доменную логику с конкретными инфраструктурными технологиями (например, PostgreSQL, MongoDB, Redis, внешние API).
events.go: Определяет структуры событий, которые этот домен может публиковать или на которые он может подписываться.
internal/middleware/
Назначение: Общие HTTP-middleware, которые могут быть применены к различным маршрутам для обеспечения сквозной функциональности (аутентификация, авторизация, CSRF-защита, лимитирование запросов).
internal/shared/
Назначение: Компоненты, которые используются несколькими доменными модулями, но при этом являются специфичными для данного приложения (не являются полностью универсальными библиотеками).
config/config.go: Определяет структуры для конфигурации приложения и логику ее загрузки (например, из переменных окружения, файлов).
common_errors/errors.go: Содержит общие типы ошибок приложения, которые могут использоваться различными доменами и API-слоем для стандартизированной обработки ошибок (например, ErrNotFound, ErrInvalidInput). Также может содержать базовые интерфейсы для доменных ошибок, позволяющие API-слою реагировать на типы ошибок, а не только на их сообщения.
validator/validator.go: Утилиты для валидации входных данных запросов, используемые в API-слое.
util/helpers.go: Вспомогательные функции, которые не привязаны к одному домену, но оперируют данными или логикой, характерной именно для "Chegonado".
internal/app_bootstrap.go
Назначение: "Корень композиции" приложения. Этот файл отвечает за инициализацию всех доменов, их зависимостей (внедрение репозиториев, сервисов и других адаптеров в соответствующие порты) и регистрацию обработчиков событий в Event Bus.
internal/routes.go
Назначение: Централизованное определение всех HTTP-маршрутов приложения. Здесь маршруты связываются с соответствующими HTTP-обработчиками из internal/ДОМЕН/api/handlers.go и применяются middleware. Также здесь настраиваются маршруты для Swagger UI.
docs/
Назначение: Этот каталог содержит автоматически сгенерированные файлы Swagger/OpenAPI 2.0 документации (swagger.json, swagger.yaml) и docs.go (файл, используемый Swaggo для инициализации). Эти файлы генерируются инструментом Swaggo на основе аннотаций в вашем коде.
Важность: Обеспечивает актуальную и легкодоступную документацию по API для разработчиков фронтенда и других потребителей.
pkg/
Назначение: Содержит общие, переиспользуемые библиотеки, которые могут быть использованы в других Go-проектах и не имеют специфической логики, привязанной к "Chegonado". Это инфраструктурные адаптеры, утилиты безопасности, общие механизмы.
pkg/infrastructure/: Компоненты, относящиеся к общей инфраструктуре:
database/: Универсальные функции для работы с подключениями к различным базам данных (PostgreSQL, MongoDB, Redis).
logger/: Настройка и обертки для стандартного логгера log/slog.
eventbus/: Реализация in-memory Event Bus.
metrics/: Инструментарий для сбора метрик (например, Prometheus).
healthcheck/: Функции для эндпоинтов проверки здоровья приложения.
pkg/security/: Общие утилиты безопасности, не привязанные к бизнес-логике:
password.go (хеширование паролей), session.go (управление HTTP-сессиями), csrf.go (CSRF-токены), data_encryption.go (шифрование данных).
pkg/paymentprovider/: Интерфейсы для взаимодействия с внешними платежными провайдерами.
pkg/email/: Утилиты для отправки электронной почты.
pkg/websocket/: Вспомогательные функции для работы с WebSocket-соединениями.
pkg/pubsub/: Дополнительные утилиты для внешних Pub/Sub брокеров (если не Event Bus, а, например, Redis Pub/Sub).
web/
Назначение: Каталог для статических файлов и шаблонов, если бэкенд также обслуживает фронтенд или генерирует HTML-страницы.
migrations/
Назначение: SQL-скрипты миграций для различных баз данных. Обеспечивает версионирование схем БД.
deploy/
Назначение: Файлы и скрипты, необходимые для развертывания приложения в производственной среде, включая конфигурации для Kubernetes, Prometheus, Grafana.
🛠️ Установка и Запуск (Локальная Разработка)
Клонируйте репозиторий:
git clone https://github.com/your-username/your-ultra-scalable-monolith.git
cd your-ultra-scalable-monolith


Установите Swaggo CLI:
go install github.com/swaggo/swag/cmd/swag@latest


Сгенерируйте документацию Swagger: После того как вы добавите аннотации Swagger к своим HTTP-обработчикам (пример см. ниже), выполните следующую команду в корне проекта:
swag init -g cmd/server/main.go --output docs # -g указывает файл, содержащий @title, @version и т.д.
Эта команда создаст или обновит файлы в каталоге docs/.
Настройте переменные окружения: Создайте файл .env в корневой директории проекта, скопировав его из .env.example, и заполните реальными значениями:
cp .env.example .env
# Откройте .env и заполните значения


Запустите базы данных с Docker Compose:
docker compose up -d


Примените миграции с помощью migrate:
go install -tags 'postgres' github.com/golang-migrate/migrate/v4/cmd/migrate@latest
migrate -path migrations/postgres -database "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}?sslmode=disable" up
# ... и для других БД, если применимо (например, для MongoDB могут быть JS-скрипты)


Интегрируйте Swagger UI в ваше приложение: В файле internal/routes.go вам нужно будет добавить маршруты для обслуживания сгенерированной документации и Swagger UI. Для этого используется пакет github.com/swaggo/http-swagger.Пример в internal/routes.go:
package internal

import (
    "net/http"

    // Импортируйте сгенерированные docs. Это критически важно для инициализации Swaggo.
    // Замените "your-app-name" на фактическое имя вашего Go-модуля (из go.mod)
    _ "your-app-name/docs" 

    "github.com/swaggo/http-swagger" // HTTP-обработчик для Swagger UI
    // ... другие импорты ...
)

// SetupRoutes настраивает все маршруты приложения
func SetupRoutes(router *http.ServeMux, authHandlers *auth.Handlers, userHandlers *users.Handlers /* ... другие хендлеры ... */) {
    // ... ваши существующие маршруты ...

    // Пример: маршруты для домена Auth
    router.HandleFunc("POST /api/v1/auth/register", authHandlers.Register)
    router.HandleFunc("POST /api/v1/auth/login", authHandlers.Login)

    // Пример: маршруты для домена Users, возможно с middleware
    // router.Handle("GET /api/v1/users/{id}", middleware.Auth(http.HandlerFunc(userHandlers.GetUserProfile)))

    // Маршруты для Swagger UI
    // Доступно по адресу http://localhost:8080/swagger/index.html
    router.HandleFunc("GET /swagger/", http.StripPrefix("/swagger", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
        http.Redirect(w, r, "/swagger/index.html", http.StatusMovedPermanently)
    })))
    router.HandleFunc("GET /swagger/{path...}", httpSwagger.Handler(
        httpSwagger.URL("/swagger/doc.json"), // URL, по которому будет доступен swagger.json
    ))
}
Убедитесь, что httpSwagger.URL указывает на правильный путь, по которому ваше приложение будет отдавать swagger.json.
Запустите Go-приложение:
go run cmd/server/main.go
После запуска вы сможете открыть документацию по адресу http://localhost:8080/swagger/index.html.
Пример использования аннотаций Swaggo
Аннотации добавляются в комментариях над функциями-обработчиками и над основной функцией (обычно main.go) для глобальной информации.
В cmd/server/main.go (или другом файле, указанном в swag init -g):
package main

import (
	"log/slog"
	"net/http"
	"os"

	"your-app-name/internal" // Замените "your-app-name"
	"your-app-name/pkg/infrastructure/logger"
	// ... другие импорты ...
)

// @title Chegonado API
// @version 1.0
// @description Документация по API для платформы "Chegonado".
// @termsOfService http://swagger.io/terms/

// @contact.name Команда разработки
// @contact.url http://www.swagger.io/support
// @contact.email support@Chegonado-clone.com

// @license.name Apache 2.0
// @license.url http://www.apache.org/licenses/LICENSE-2.0.html

// @host localhost:8080
// @BasePath /api/v1
// @securityDefinitions.apikey ApiKeyAuth
// @in header
// @name Authorization
func main() {
	// ... ваша логика main ...
}


В internal/users/api/handlers.go (над функцией-обработчиком):
package api

import (
	"net/http"
	"your-app-name/internal/middleware" // Замените "your-app-name"
	"your-app-name/internal/shared/common_errors"
	"your-app-name/internal/users/domain" // Предполагается, что доменные ошибки определены здесь
	"your-app-name/internal/shared/validator"
)

// @Summary Получить профиль пользователя
// @Description Получает информацию о профиле пользователя по его ID.
// @Tags users
// @Accept json
// @Produce json
// @Param id path string true "ID пользователя"
// @Security ApiKeyAuth
// @Success 200 {object} object{user=domain.UserResponse} "Успешный ответ с профилем пользователя"
// @Failure 400 {object} common_errors.ErrorResponse "Неверный ID пользователя"
// @Failure 404 {object} common_errors.ErrorResponse "Пользователь не найден"
// @Failure 500 {object} common_errors.ErrorResponse "Внутренняя ошибка сервера"
// @Router /users/{id} [get]
func (h *Handlers) GetUserProfile(w http.ResponseWriter, r *http.Request) {
	// ... ваша логика обработчика ...
}

// @Summary Зарегистрировать нового пользователя
// @Description Регистрирует нового пользователя в системе.
// @Tags auth
// @Accept json
// @Produce json
// @Param user_data body auth.RegisterRequest true "Данные для регистрации пользователя"
// @Success 201 {object} object{message=string} "Пользователь успешно зарегистрирован"
// @Failure 400 {object} common_errors.ErrorResponse "Неверные входные данные или пользователь с таким email уже существует"
// @Failure 500 {object} common_errors.ErrorResponse "Внутренняя ошибка сервера"
// @Router /auth/register [post]
func (h *Handlers) Register(w http.ResponseWriter, r *http.Request) {
    // ...
}


